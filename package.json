{
  "name": "s3renity",
  "version": "0.5.0",
  "description": "Promise-based S3 api wrapper with batch operations (forEach, map, reduce, filter)",
  "main": "src/index.js",
  "author": "Wells Johnston",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/littlstar/s3renity"
  },
  "scripts": {
    "test": "node test",
    "docs": "rm -rf docs/ && node_modules/jsdoc/jsdoc.js -c jsdoc.json -R frontpage.md"
  },
  "dependencies": {
    "aws-sdk": "^2.2.17",
    "ink-docstrap": "^1.1.0",
    "mock-aws-s3": "^2.0.0"
  },
  "devDependencies": {
    "jsdoc": "^3.4.0",
    "tape": "^4.4.0"
  }
}
